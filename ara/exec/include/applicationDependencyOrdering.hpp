#pragma once
#include <bits/stdc++.h>        
#include"application.hpp"

using namespace std;

namespace ara
{

    namespace exec
    {

        class ApplicationDependencyOrdering
        {
            // public:
            // typedef map<string,Application::DependencyCollectionType> DependencyMap;
            // //contains all dependencies of processes
            // //"process1" -> "x" -> kRunning
            // //           -> "y" -> kIdle
            // //"process2" -> "m" -> kTerminating
            // //           -> "l" -> kIdle
            // //           -> "z" -> kRunning
            // DependencyMap dependencies_;

            // private:
            // //Not sure of the argument..
            // ApplicationDependencyOrdering(vector<Application>);
            // bool IsValid();
            // //check this
            // bool operator()(left,right);

        };
    }
}
